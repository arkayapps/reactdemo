# React App Assignment - 1
- Build using ReactJS to fetch data from StackOverFlow website using StackExchange APIS

# Feature
1. Infinite scroll Question without noticeable paging.
2. Dark Mode Support
3. React.js use
4. The app display be a single page with the list with the following columns:
	- “Author”
	- “Title”
	- “Creation date”
	- “Author Profile Photo”
4. 	Pop up with question title and text with link to redirect stackoverflow url.

## Live demo link
- [React App](https://test1.adhyaynam.in/)

## Available Scripts

In the project directory, you can run:

### `npm install`
Installs all the required npm packages for the project.

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

The page will reload if you make edits.\
You will also see any lint errors in the console.

### `npm run build`

Builds the app for production to the `build` folder.\
It correctly bundles React in production mode and optimizes the build for the best performance.

The build is minified and the filenames include the hashes.\
Your app is ready to be deployed!
